import greatest_num_using_function
greatest_num_using_function.max(10,20,30)


#we can import the function or method by using __main__ Method
#   if __name__ == __main__:

# by using that condition we can acces only the functions

#then we can directly call the function in another file
#imported file name start with alfabet